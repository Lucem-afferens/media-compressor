# Optional transcription stack (install alongside requirements.txt)
-r requirements.txt
faster-whisper>=1.0
stable-ts>=2.19
httpx>=0.27
# Full tier only (vocal separation + torch):
# demucs>=4.0
# torch>=2.0
